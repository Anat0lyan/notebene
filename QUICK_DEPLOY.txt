═══════════════════════════════════════════════════════════════
  КОМАНДЫ ДЛЯ ДЕПЛОЯ - БЫСТРАЯ ШПАРГАЛКА
═══════════════════════════════════════════════════════════════

📦 BACKEND (Production)
───────────────────────────────────────────────────────────────

cd server
npm install --production=false
npm run build
npm start

Или одной строкой:
cd server && npm install --production=false && npm run build && npm start


🌐 FRONTEND (Production)
───────────────────────────────────────────────────────────────

cd client
npm install
npm run build

Или одной строкой:
cd client && npm install && npm run build


🔧 DEVELOPMENT (Разработка)
───────────────────────────────────────────────────────────────

Backend:
cd server && npm install && npm run dev

Frontend:
cd client && npm install && npm run dev


✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ
───────────────────────────────────────────────────────────────

curl http://localhost:3000/api/health
# Должен вернуть: {"status":"ok"}


📝 ВАЖНО
───────────────────────────────────────────────────────────────

1. Backend требует dev зависимости для сборки TypeScript
2. После сборки backend код будет в server/dist/
3. После сборки frontend файлы будут в client/dist/
4. Убедитесь что создан файл server/.env с переменными окружения

═══════════════════════════════════════════════════════════════

