═══════════════════════════════════════════════════════════════
  КОМАНДЫ ДЛЯ ДЕПЛОЯ - БЫСТРАЯ ШПАРГАЛКА
═══════════════════════════════════════════════════════════════

🚀 ПОЛНЫЙ ДЕПЛОЙ (Backend отдает Frontend)
───────────────────────────────────────────────────────────────

# 1. Собрать фронтенд
cd client && npm install && npm run build

# 2. Собрать и запустить бэкенд (отдает статику)
cd ../server && npm install --production=false && npm run build && NODE_ENV=production npm start

Или используйте скрипт:
./build.sh
cd server && NODE_ENV=production npm start

Результат: Один сервер на порту 3000 обслуживает и API (/api/*) и фронтенд


📦 BACKEND ТОЛЬКО (без фронтенда)
───────────────────────────────────────────────────────────────

cd server
npm install --production=false
npm run build
npm start

Или одной строкой:
cd server && npm install --production=false && npm run build && npm start


🌐 FRONTEND ОТДЕЛЬНО (для разработки)
───────────────────────────────────────────────────────────────

cd client
npm install
npm run dev


🔧 DEVELOPMENT (Разработка)
───────────────────────────────────────────────────────────────

Backend:
cd server && npm install && npm run dev

Frontend:
cd client && npm install && npm run dev


✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ
───────────────────────────────────────────────────────────────

# API
curl http://localhost:3000/api/health
# Должен вернуть: {"status":"ok"}

# Frontend (если NODE_ENV=production)
curl http://localhost:3000/
# Должен вернуть HTML страницу


📝 ВАЖНО
───────────────────────────────────────────────────────────────

1. Backend отдает статику только если:
   - NODE_ENV=production ИЛИ
   - SERVE_STATIC=true

2. Backend требует dev зависимости для сборки TypeScript

3. Порядок сборки:
   - Сначала соберите frontend (client/dist/)
   - Затем соберите backend
   - Запустите backend с NODE_ENV=production

4. Убедитесь что создан файл server/.env с переменными окружения

═══════════════════════════════════════════════════════════════
