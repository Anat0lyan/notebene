═══════════════════════════════════════════════════════════════
  КОМАНДЫ ДЛЯ DEPLOY НА RENDER.COM
═══════════════════════════════════════════════════════════════

⚠️  ПРОБЛЕМА: Фронтенд не собирается перед запуском сервера

✅ РЕШЕНИЕ: Обновите команды деплоя


📦 BUILD COMMAND (команда сборки)
───────────────────────────────────────────────────────────────

cd client && npm install && npm run build && cd ../server && npm install --production=false && npm run build


🚀 START COMMAND (команда запуска)
───────────────────────────────────────────────────────────────

cd server && NODE_ENV=production npm start


📋 ОДНОЙ КОМАНДОЙ (Build & Start вместе)
───────────────────────────────────────────────────────────────

cd client && npm install && npm run build && cd ../server && npm install --production=false && npm run build && NODE_ENV=production npm start


🔧 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ
───────────────────────────────────────────────────────────────

NODE_ENV=production
DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=notabene
DB_USER=your_db_user
DB_PASSWORD=your_db_password
JWT_SECRET=your-secret-key


✅ ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ
───────────────────────────────────────────────────────────────

API: curl https://your-app.onrender.com/api/health
Frontend: Откройте https://your-app.onrender.com в браузере

═══════════════════════════════════════════════════════════════

